version: '3.5'

services:

  postgres:
    iamge: postgres:latest
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=hom

  editora:
    image: gabazar/editora-api:latest
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - "8080:8080"
    environment:
      - POSTGRES_URL=jdbc:postgresql:5432/hom
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    depends_on:
      - postgres

volumes:
  - postgres-data